import { FreehandDrawDirective } from './freehand-draw.directive';

describe('FreehandDrawDirective', () => {
  it('should create an instance', () => {
    const directive = new FreehandDrawDirective();
    expect(directive).toBeTruthy();
  });
});
